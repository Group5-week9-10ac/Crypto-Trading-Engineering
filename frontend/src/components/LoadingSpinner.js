import React from 'react';
import './LoadingSpinner.css'; // External CSS for styling

const LoadingSpinner = () => (
    <div className="spinner-container">
        <div className="spinner"></div>
    </div>
);

export default LoadingSpinner;
